# Terraform lookup() Function Example

This project demonstrates use of the Terraform `lookup()` function.

- `lookup()` retrieves a value from a map with a fallback default.
- Tested using `terraform console` and output blocks.
- Includes an AWS data source example for extra credit.

## Commands Used
```bash
terraform init
terraform apply -auto-approve
terraform console
